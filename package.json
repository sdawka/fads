{
  "name": "fads",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler deploy",
    "check": "npm run format:check && npm run lint && npm run typecheck && npm run test",
    "typecheck": "astro check && tsc --noEmit",
    "lint": "eslint .",
    "format:check": "prettier --check .",
    "test": "npm run test:unit && vitest run --config vitest.workers.config.ts",
    "test:unit": "vitest run test/unit",
    "test:real-worker": "npm run build && vitest run --config vitest.real-worker.config.ts",
    "test:typecheck": "tsc -p test/tsconfig.json --noEmit",
    "test:e2e": "playwright test",
    "cf-typegen": "wrangler types src/worker-configuration.d.ts"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.2.5",
    "@astrojs/svelte": "^9.0.1",
    "@atcute/bluesky": "^4.0.20",
    "@atcute/client": "^5.1.1",
    "@atcute/identity-resolver": "^2.0.1",
    "@atcute/oauth-node-client": "^2.0.1",
    "astro": "^7.2.9",
    "fast-xml-parser": "^5.11.1",
    "htmlparser2": "^12.0.0",
    "ipaddr.js": "^2.5.0",
    "svelte": "^5.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@axe-core/playwright": "^4.13.0",
    "@cloudflare/vitest-plugin": "^1.0.0",
    "@eslint/js": "^9.0.0",
    "@playwright/test": "^1.57.0",
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "eslint-plugin-import": "^2.31.0",
    "fast-check": "^4.9.0",
    "prettier": "^3.0.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^4.1.0",
    "wrangler": "^4.0.0"
  }
}
